
## [MEDIUM] [LEFTOVERS pod3b] Qwen3-Omni 3b: the cosine 0.0051 now has a twin, and the Mac refit 0.7864 vs 0.8307 comes from a scikit-learn fold change (2026-09-23)
- lm_head: thinker.lm_head.weight of Qwen/Qwen3-Omni-30B-A3B-Instruct, revision 26291f79, shard 13, fetched on Linux pods two ways (HTTP range read of that tensor only; the full shard through huggingface_hub and safetensors). sha256 of the bf16 bytes is ef1c8244... on both. The direction built from it matches POD3's saved proj_d to 1.4e-6 on all 468 clips, so it is the same d.
- Cosine, from direction_by_arm.py run unmodified with the float16 lm_head dump POD3 used: all 0.0051 (0.005109), conflict -0.0067, agreement 0.0086. The exact bf16 weights give the same to 8 dp. Independent verifier: 0.005109, -0.006677, 0.008638. Random floor 0.0177 [0.0008, 0.0498].
- auc_d and auc_without_d, marked UNVERIFIABLE ON MAC in POD3_partF_out.json, now reproduce: all 0.7656 [0.7088, 0.8179]; without d 0.8305 [0.7826, 0.8737] with train-fold z, 0.8276 [0.7795, 0.8696] with held-out z. Conflict 0.6029 / 0.6196 / 0.6158, agreement 0.8264 / 0.9341 / 0.9326.
- Refit: on Linux (scikit-learn 1.9.1) the probe gives 0.8307 [0.7833, 0.8741] and matches POD3's saved OOF to 6e-8 per clip. The Mac refit (scikit-learn 1.7.2) gives 0.7864 [0.7335, 0.8359].
- Cause: GroupKFold. scikit-learn 1.9.0 changed GroupKFold to sort speaker sizes with a stable sort (PR 28464, in the v1.9 changelog; the 1.7.2 and 1.8.0 sources use the default sort). The 228 speakers have only 8 distinct clip counts (112 have one clip), so the tie order decides the folds, and all 228 speakers get different fold-mates. Checked both ways: the Linux fit on the Mac folds gives 0.7864, and the Mac fit on the Linux folds gives 0.8307. Conflict 0.6145 vs 0.5520 and agreement 0.9349 vs 0.9314 are the same effect. The real 1.7.2 code run on Linux gives a third split (0.7973), so under 1.7.2 the folds also depend on the machine.
- What 0.8307 is: one favourable split. Over 200 splits of the same rule that differ only in tie order, the probe averages 0.8033 (sd 0.0122, middle 95% 0.7786 to 0.8244). Only 2 of 200 reach 0.8307. The cosine stays between 0.0023 and 0.0090 on every split, always inside the random floor.
- Paper line 149 says removing the readout direction leaves the Qwen3-Omni probe at 0.83. That is true on POD3's split (0.8305 / 0.8276). Averaged over the 200 splits it is 0.81 / 0.80, and the probe itself averages 0.80, so removing d still changes nothing (mean change -0.003 / +0.003). The cosine sentence (0.005) holds on every split.
- Files: scores/leftovers_23sep/pod3b/pod3b_values.tsv (121 rows, all twinned at 4 dp) and pod3b_values.sidecar.json.
